{
  "name": "surge-vector-sorter",
  "version": "1.0.0",
  "private": true,
  "description": "芯片扫描向量浪涌约束闭环加载顺序全局裁决工具（纯静态站点）",
  "type": "module",
  "scripts": {
    "test": "node --test test/solver.test.mjs test/ramping.test.mjs",
    "build-check": "node scripts/build-check.mjs",
    "smoke": "node test/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
